/* eslint-disable no-undef */
import { generateHyperdriveAnimation } from '../DetailPage.util'

describe('generateHyperdriveAnimation', () => {
  it('???', () => {
    // not sure what exactly I should be testing here.
    generateHyperdriveAnimation({})()

    expect().toHaveBeenCalledWith()
  })
})
